# Dev dependencies used to run and build WhisperDesk (end users of the .exe don't need these)

# GUI
PySide6>=6.7

# Transcription
# Note: Whisper v3 models require 128 mel features (faster-whisper>=1.0 + ctranslate2>=4.6)
# Pin to 1.1+ to avoid older Windows wheels that default to 80 mels
faster-whisper>=1.1.0
ctranslate2>=4.6.0

# Diarization
onnxruntime>=1.16.0
webrtcvad>=2.0.10
scikit-learn>=1.4.0
huggingface_hub>=0.22.0
certifi>=2024.2.0
requests>=2.31.0
urllib3>=2.0.0
# SpeechBrain for high-quality speaker embeddings
# Use 1.0.1+ for huggingface_hub >=0.20 compatibility (token parameter)
speechbrain>=1.0.1
# PyTorch - compatible with SpeechBrain 1.0.1+
torch>=2.0.0,<2.5.0
torchaudio>=2.0.0,<2.5.0

# Audio/processing
numpy>=1.25,<2.0
soundfile>=0.12
scipy>=1.11.0

# Exporters
python-docx>=1.0.0

# System info
psutil>=5.9
pynvml>=11.0.0

# Build (Windows)
pyinstaller>=6.0
pyinstaller-hooks-contrib>=2024.0
